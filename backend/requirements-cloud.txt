# Cloud deployment requirements for Render/Railway/Heroku
# This file ensures proper system packages are installed

# System packages needed for python-magic
# These translate to different package managers:
# - apt-get: libmagic1 libmagic-dev
# - yum: file-devel
# - brew: libmagic

# Python package that requires the system library
python-magic==0.4.27

# Include all other dependencies
-r requirements.txt
