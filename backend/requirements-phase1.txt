# Phase 1 Requirements for Railway Deployment - Regex/Keyword Detection Only
# Core Flask dependencies
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7
gunicorn==21.2.0

# Authentication
flask-jwt-extended==4.6.0
python-dotenv==1.0.0

# Supabase integration (Phase 1 database)
supabase==2.0.2
postgrest==0.10.8

# Security
bcrypt==4.0.1

# HTTP client
requests==2.31.0
httpx==0.24.1

# JSON handling
python-dateutil==2.8.2

# NOTE: Removed scikit-learn, numpy, joblib for Phase 1
# Phase 1 uses only regex/keyword-based profanity detection
# ML dependencies will be added in Phase 2
